[package]
name = "gently-micro"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Micro Infrastructure - Local intelligence layer mirroring the macro network"

[dependencies]
# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Crypto (for hashing)
sha2 = { workspace = true }

# Time
chrono = { workspace = true }

# IDs
uuid = { workspace = true }

# Pattern matching
regex = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Hex encoding
hex = { workspace = true }

# Lazy initialization
lazy_static = { workspace = true }

# File system
dirs = { workspace = true }

# Internal crates
gently-core = { workspace = true }
gently-alexandria = { workspace = true }
gently-search = { workspace = true }
gently-inference = { workspace = true }

[dev-dependencies]
tempfile = "3.10"
